name: hurrlab
channels:
  - conda-forge
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - numpy
  - scipy
  - matplotlib
  - tqdm
  - scikit-learn
  - networkx
  - pyyaml
  - basemap
  - tigramite
  - pytorch
  - pip
  # ---- If you have NVIDIA GPU with CUDA 11.8, uncomment the next two lines ----
  # - pytorch-cuda=11.8
  # - cudatoolkit=11.8
  # ---------------------------------------------------------------------------
  # Otherwise for CPU-only, PyTorch above suffices.
  - pip:
      # keep pip section empty or add extras if needed
      - 
